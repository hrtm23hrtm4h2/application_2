<div class="container">
  <div class="row">
  <div class="col-xs-6 col-xs-offset-3">

      <h1>Editing Book</h1>
      <h4>Title</h4>
    <%= form_for (@book) do |f| %>
      <%= f.text_field :title %>
      <h4>Opinion</h4>
      <%= f.text_area :body %><br>
      <%= f.submit 'Update Book', class: "btn btn-primary" %>
    <% end %>
      <div>
    <%= link_to "Show", book_path(@book) %>|
    <%= link_to "Back", users_path(@book) %>
 </div>
   </div>
  </div>
</div>