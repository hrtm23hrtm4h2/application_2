        <h2>New book</h2>

        <%= form_for(book) do |f| %> <!-- @book → book -->

         <div class="form-group">
          <h4>Title</h4>
           <%= f.text_field :title %><br>
          <h4>Opinion</h4>
           <%= f.text_area :body %><br>
           <%= f.submit "Create Book", class: "btn btn-default btn-block" %>
         
         </div>
        <% end %>
      